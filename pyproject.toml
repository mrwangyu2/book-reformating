[project]
name = "epub-reformat"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "ebooklib>=0.18",
    "Pillow>=10.0",
]

[project.scripts]
epub-reformat = "cli:main"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["cli", "epub_processor", "image_processor"]
